/* package fr.ca.cat.pocdafne;

import static io.restassured.RestAssured.given;
import org.junit.jupiter.api.Test;
import io.quarkus.test.junit.QuarkusTest;

@QuarkusTest
public class AvisResourceTest {

    @Test
    public void testAvisEndpoint() {
        given()
          .when().get("/avis")
          .then()
             .statusCode(200);
    }

} */